// script.Event.Total
// This script requires ALL flagged Remote Printer items to be sent to Remote Printer before Payment
//
//  # SmartPOS - Software de punto de venta touch - Creado por Alejandro Camargo retomando trabajo de código abierto de Openbravo y smartpos. Todos los derechos Reservados 
// **************************************************************************


for(int i= 0; i < ticket.getLinesCount(); i++){
	line = ticket.getLine(i);
	if ($ticketline.isProductKitchen()){
		line.setProperty("sendstatus", "No");
	}
	if(line.getProperty("sendstatus").equals("No")){
		mySent = "No";
	}
}

if (mySent == "No"){
	javax.swing.JOptionPane.showMessageDialog(null, "Antes de cerrar el ticket porfavor envia la orden a la impresora", "Enviar", JOptionPane.WARNING_MESSAGE);
	return "Cancelar";
}else{
	return null;
}
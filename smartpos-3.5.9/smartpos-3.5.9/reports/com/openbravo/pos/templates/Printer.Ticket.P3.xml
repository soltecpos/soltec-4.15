<?xml version="1.0" encoding="UTF-8"?>
<!--
SmartPOS - Punto de Venta Touch - Copyright Alejandro Camargo 2018 - (c) Unisen, OpenBravo, Chromun

-->

<output>
    <display>
        <line>
            <text align="left" length="10">Orden enviada a  P3</text>
            <text align="right" length="10">${ticket.printTotal()}</text>
        </line>
        <line>
            <text align="center" length="20">Gracias.</text>
        </line>
    </display>

    <ticket printer="3">
        <line></line>
        <line></line>
        <line size = "1"><text bold = "true" align="center" length="42">Impresora 3</text></line>
        <line></line>
        <line size = "1"><text>Orden : </text><text bold = "true">${ticket.getPickupId()}</text></line>

        <line><text align="left" length="15">Fecha:</text><text>${ticket.printDate()}</text></line>
        <line><text align="left" length="15">Enviado desde: </text><text>${ticket.getHost()}</text></line> 
        <line><text align="left" length="15">Servidor:</text><text>${ticket.printUser()}</text></line>

        #if ($ticket.getCustomer())
            <line><text align="left" length="15">Cliente:</text><text>${ticket.getCustomer().getName()}</text></line>
            <line><text align="left" length="15">Cliente #:</text><text>${ticket.getCustomer().getTaxid()}</text></line>
        #end

        #if (${tickettext.place} != ${place})
            <line><text align="left" length="15">Mesa:</text><text>${place}</text></line>
        #end
        
        <line></line>
        <line><text align ="left" length="17">Item</text><text align ="right" length="5"></text></line>
        <line><text>------------------------------------------</text></line>

        #foreach ($ticketline in $ticket.getLines())
		${ticketline.setProperty("ticket.borrable", "no")}
            #if($ticketline.printPrinter().equals("3"))
                #if($ticketline.getProperty("ticket.updated").equals("true"))
                    #if ($ticketline.isProductCom())
                        <line size="1"><text align ="left" length="37">--${ticketline.printName()}</text></line>
                    #else
                        <line size="1">
                        <text align ="left" length="5"> ${ticketline.printMultiply()}x</text>
                        <text align ="left" length="37"> ${ticketline.printName()}</text>
                        </line>
                    #end

                    <!-- un-comment line below if you want to add a product's properties -->
                    <!-- <line><text align ="left" length="42">${ticketline.getProperty("ingredients", "")}</text> </line> -->

                    <!-- un-comment line below if you want to add a user input sales line's Note -->
                    <!-- <line><text align ="left" length="42">${ticketline.getProperty("notes", "")}</text> </line> -->      

            <!-- Add the following lines only for 2.30 Attributes -->
                    #if ($ticketline.productAttSetInstId)
                        <line><text align ="left" length="42">${ticketline.productAttSetInstDesc}</text></line>
                        <line></line>
                    #end
                    ${ticketline.setProperty("ticket.updated", "false")}
                #end
            #end
        #end
    </ticket>
</output>